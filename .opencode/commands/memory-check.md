---
description: Validate INTERNAL_RAG (index + validate + status + guard)
---

Load internal-rag. Run `mlm.py index`, then `mlm.py status`, `mlm.py validate`, then `mlm.py guard`. Resolve stale checkpoint state before finishing. Use `mlm.py doctor` for full diagnostics.