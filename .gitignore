__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
dist/
build/
*.zip
*.log
.DS_Store
Thumbs.db

# Project-specific operational memory — always local, never committed.
# (The tooling recreates this directory automatically via `irag.py init` /
# `checkpoint`, so no template or skeleton is needed in the repo.)
INTERNAL_RAG/
.irag.yml
.venv-embed/
